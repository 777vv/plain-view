# Plain View 重大更新:从文件查看器到轻量文本工作台,一个浏览器扩展就够了

距离 Plain View 第一版发布已经过去了几个月。当时我在公众号分享了这个小工具——把 Chrome 浏览器变成 JSON、Markdown、SQL、YAML、CSV、LOG 六种文件格式的友好查看器。零运行时依赖、纯手写 TypeScript、无打包器,整个扩展只有 60 多 KB。

那篇文章发出后收到了不少反馈,有朋友说「能不能加个翻译功能」、「能不能对比两段文本」、「能不能做个二维码生成」。我一想,这些需求本质上都是「在浏览器里处理文本」——那为什么不直接在工作台里做呢?

于是就有了这次的重大更新。新版 Plain View 在文件查看器能力之上,增加了一个**内置工作台(Playground)**,集成了 JSON 格式化、Markdown 实时预览、SQL 美化、Base64 编解码、URL 解析、翻译、文本对比、二维码生成、备忘录等功能。下面逐一介绍。

---

## 工作台总览

打开方式很简单:点击浏览器工具栏的 Plain View 图标,在弹窗里点「打开工作台」。建议右键图标选「固定」,以后一键打开。

工作台采用**左右分栏布局**:左侧输入、右侧输出。顶部有一排功能切签(Chips),点击即可切换模式。所有可编辑的区域都有**行号**和**当前行高亮**,支持拖拽文件到输入框自动加载。

> 📷 **截图 1**:工作台总览,显示顶部切签 + 左右分栏

最右侧的工具栏提供复制结果、清空、调节字号、固定提示、查看源码等按钮。弹窗里还有一个「功能开关」面板,可以随时开关工作台里的功能模块——用不上的功能关掉即可,不会影响文件查看器本身。

---

## 功能一:JSON 格式化

这是工作台里用得最多的功能。左侧粘贴 JSON,右侧实时展示格式化结果。支持四种视图:

- **格式化**:pretty-print,带语法高亮和行号,key/string/number/boolean/null 各有配色
- **压缩**:去掉所有空白,变为一行(显示上自动换行,但复制时是不换行的原文)
- **转义**:把 JSON 转为可以在 JS 字符串里使用的转义形式
- **去转义**:反向操作,把转义后的字符串还原

如果 JSON 格式有误,右侧不会空白静默——会显示一个友好的错误占位,同时自动定位到出错的行列号,告诉你问题在哪。

> 📷 **截图 2**:JSON 界面,左侧输入,右侧格式化 + 行号 + key/value 彩色

---

## 功能二:Markdown / SQL 实时预览

这两个模式本质上是「所见即所得」。左侧写 Markdown 或 SQL,右侧即时渲染高亮结果。

Markdown 支持标题、列表、表格、代码块、任务列表等常见语法,危险协议(`javascript:` / `data:`)在渲染时自动屏蔽,没有 XSS 风险。

SQL 模式自动格式化 `SELECT` / `INSERT` / `UPDATE` / `CREATE TABLE` 等语句,关键字、字符串、数字、注释各用不同颜色区分,带行号,长语句有横向滚动条。

> 📷 **截图 3**:Markdown 或 SQL 界面

---

## 功能三:翻译

这是我收到最多的需求。工作台里内置了翻译功能,底层调用免费翻译 API。

使用方式:左侧输入文本,点「英译中」或「中译英」按钮,右侧出结果。按钮就在输出区域顶部,蓝底白字,和 Plain View 的品牌色一致。长文本会自动分段逐片翻译,不用担心字符超限。

两个按钮的语义很明确:「英译中」走 en→zh 通道,「中译英」走 zh→en 通道。翻译结果支持复制,切出翻译模式再切回来,如果左侧文本没变不会重复请求,直接展示上次结果。

需要说明的是,翻译 API 有日配额限制(500 字符/天/IP),超出后会提示次日再试。这个限制来自上游服务,和扩展无关。

> 📷 **截图 4**:翻译界面,左侧英文,右侧中文结果,显示英译中/中译英按钮

---

## 功能四:文本对比

这个功能是仿 IntelliJ IDEA 的对比视图设计的。左右两个编辑框并排,中间一条窄槽。输入两段文本后自动逐行对比:

- **绿色行**:右侧新增
- **红色行**:左侧被删除
- **黄色行**:两边都有但内容不同
- 黄色行内还有**词级高亮**,精确到具体哪些字符变了
- 中间窄槽在变更行处显示**← / → 箭头**,点击即可用一侧覆盖另一侧

顶部工具栏显示差异统计(+N / -N / =N),可以交换两侧、全部用左、全部用右。编辑框内置行号,滚动左右同步。

对于经常需要对比配置文件、代码 diff、文章修改稿的开发者来说,这个功能应该很实用。

> 📷 **截图 5**:文本对比界面,左右有差异行红绿染色,词级高亮,中间箭头

---

## 功能五:二维码生成

输入任意文本或 URL,自动生成二维码。二维码用 DOM 渲染(每个模块是一个 `<b>` 标签),确保像素级别清晰,手机相机扫码识别率比 Canvas 渲染高不少。

底部有一键「下载 PNG」按钮,蓝底白字,用的是 Plain View 的品牌色。二维码采用 mask 7 + M 级纠错,经过多轮实测验证了扫码可靠性。

> 📷 **截图 6**:二维码界面,有生成的二维码 + 版本/尺寸信息

---

## 功能六:备忘录

这是一个轻量级的双栏纯文本便签。左右两栏各自独立,内容自动保存到 Chrome 本地存储(清除浏览器缓存不会丢失,卸载扩展才会丢)。关掉标签页再打开,之前写的内容还在。

顶栏右侧有「导出」按钮,可以把两栏内容分别导出为 TXT 文件。支持行号和当前行高亮,写长文时定位方便。

备忘录适合随手记录、临时粘贴、对比两段文字等场景。要说它的定位,就是「不是 Notion,也不打算是 Notion」——就是一页便签,打开即用,无学习成本。

> 📷 **截图 7**:备忘录界面,左右两栏都有文字,显示导出按钮

---

## 还有这些细节

除了上面 6 个主要功能,还有一些值得一提的小改进:

- **Base64 编解码**:自动检测方向,支持编码和解码两种模式切换
- **URL 解析**:解码 URL 的同时,把协议、主机、路径、查询参数拆成表格展示
- **拖拽文件**:把 `.json`、`.md`、`.sql` 文件直接拖到工作台输入框,自动识别格式并加载内容
- **行号 + 当前行高亮**:所有可编辑区域都有行号,光标所在行有淡蓝色背景高亮,跟桌面 IDE 体验一致
- **输出区统计**:每种格式渲染完成后,输出区顶部显示「N 行 · M 字符」的统计信息
- **弹窗功能开关**:弹窗列出了全部功能模块,不想用的关掉即可,对应的芯片会在工作台里隐藏,保持界面清爽
- **品牌视觉升级**:图标从绿色改为品牌蓝(#0550ae),工作台的工具栏、面板标题、功能按钮沿袭同色系,亮白/暗黑双主题都有适配
- **中英文双语**:自动检测浏览器语言,中文环境显示中文,其余显示英文

---

## 技术实现

和第一版保持一致,整个扩展仍然是零运行时依赖。新增的工作台、编解码器、QR 生成器全部手写:

```
src/
├── playground/      # 工作台核心(入口 + diff 算法 + diff 视图)
│   ├── index.ts     # 主逻辑,约 1200 行
│   ├── diff.ts      # Myers 行级 diff + 词级 diff
│   └── diffView.ts  # IDEA 风格并排对比视图
├── decoders/        # 编解码器
│   ├── base64.ts    # Base64 编解码
│   ├── url.ts       # URL 解析
│   └── qr.ts        # QR Code 生成(手写 Reed-Solomon 纠错)
├── popup/           # 弹窗 + 功能开关
└── ui/              # 共享组件:toolbar、themes、fontSize、i18n
```

QR 码生成器值得一提:完全从零手写,实现了字节模式编码、Reed-Solomon 纠错码、8 种掩码、格式信息 BCH 编码等功能,支持 Version 1-10,纠错级别 M(15%)。开发过程中踩了不少坑——比如 dark module 被 format info 覆盖导致扫码失败、不同环境下 penalty 函数打分不一致导致选错掩码等,这些都是标准文档里不会告诉你的实战细节。

工作台的文本对比功能也实现了一个完整的 Myers 行级 diff 算法加 LCS 词级 diff,对比结果的渲染用了三个叠加层(背景染色层、gutter 行号层、textarea 编辑层)来模拟 IDEA 的视觉效果。这部分的代码在 `src/playground/diffView.ts`,对 diff 算法感兴趣的朋友可以直接看源码。

---

## 安装和源码

**普通用户**:直接下载已编译的 zip,无需 Node.js 环境:

- GitHub Release: https://github.com/777vv/plain-view/releases
- Gitee(国内更快): https://gitee.com/vv777/plain-view/releases

解压后在 `chrome://extensions` 开启「开发者模式」,加载已解压的扩展程序即可。Edge 浏览器同理(`edge://extensions`)。

**开发者**: clone 源码后 `npm install && npm run build`:

- GitHub: https://github.com/777vv/plain-view
- Gitee: https://gitee.com/vv777/plain-view

编译产出在 `dist/`,然后用项目根目录加载扩展。`npm run package` 可以打 zip 包用于发布。

---

## 最后

Plain View 从最初的「六种文件格式的查看器」到现在的「文件查看器 + 多功能文本工作台」,核心理念一直没变:**轻量、零依赖、开箱即用**。

如果你平时经常在浏览器里查看 JSON 文件、对比文本差异、临时翻译一段文字、生成一个二维码——不用再去找在线工具了,点一下扩展图标就能搞定。

欢迎试用、Star、提 Issue 或 PR。有什么想法,也可以在公众号留言告诉我。
